import 'package:flutter/material.dart';

class NodeInfo extends StatelessWidget {
  const NodeInfo({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
